const producto = {
    nombre: 'Monitor 20 pulgadas',
    precio: 300,
    disponible: true,
}

console.log(Object.keys(producto)); // Imprime las llaves del objeto producto

console.log(Object.values(producto)); // Imprime los valores del objeto producto

console.log(Object.entries(producto)); // Imprime las llaves y los valores del objeto producto