// Pure Functions

const numero1 = 20;
const duplicar = numero => numero * 2; // Pure function no modifican el valor original

const resultado = duplicar(numero1);


console.log(resultado);
console.log(numero1);